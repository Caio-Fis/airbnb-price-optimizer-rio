# MLflow
# Deixe MLFLOW_TRACKING_URI vazio para usar o SQLite local: src/training/mlflow_setup.py
# monta a URI a partir do caminho absoluto SEM url-quoting. O default do MLflow
# faz o quoting e, se o diretório do projeto tiver espaços, grava num banco-sombra.
MLFLOW_TRACKING_URI=
MLFLOW_EXPERIMENT_NAME=airbnb-price-optimizer

# Deploy — Hugging Face Spaces (requer `hf auth login`)
HF_SPACE_ID=Caio-Fis/airbnb-price-optimizer-rio

# Airflow
AIRFLOW_UID=50000
_AIRFLOW_WWW_USER_USERNAME=admin
_AIRFLOW_WWW_USER_PASSWORD=admin
# Gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=your-fernet-key-here

# PostgreSQL (shared by Airflow and MLflow)
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow

# API
API_HOST=0.0.0.0
API_PORT=8000
MODEL_PATH=/app/models/model.joblib
ENCODERS_PATH=/app/models/encoders.joblib

# Security
# Gere com: python -c "import secrets; print(secrets.token_hex(32))"
API_KEYS_RAW=your-secret-key-1,your-secret-key-2
ENVIRONMENT=production        # development | production (desabilita /docs em prod)
ALLOWED_ORIGINS=https://yourdomain.com   # separar por vírgula; * apenas em dev
RATE_LIMIT_PREDICT=30/minute
RATE_LIMIT_HEALTH=120/minute

# Data
RAW_DATA_PATH=data/raw
PROCESSED_DATA_PATH=data/processed
IMAGES_PATH=data/images
INSIDE_AIRBNB_CITY=rio-de-janeiro
